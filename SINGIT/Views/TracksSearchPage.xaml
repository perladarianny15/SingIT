<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SINGIT.Views.TracksSearchPage">
    <ContentPage.Content>
    <StackLayout Padding="20">
        <Button Text="GetTracks" 
                     Command="{Binding GetDataCommand}" BackgroundColor="Black" TextColor="White" HorizontalOptions="FillAndExpand"/>

        <ListView x:Name="TracksList" ItemsSource="{Binding Tracks}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding TrackName}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
    </ContentPage.Content>
</ContentPage>